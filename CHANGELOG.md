# Changelog

## Unreleased

- Repository skeleton: two-layer architecture (`Latchkey::Core` /
  `Latchkey::Rails::Engine`), closed `Result` type, challenge adapter
  interface (`Base`/`Null`/`Test`), generator stubs (`install`, `views`,
  `controllers`, `javascript`, `challenge`), `latchkey:doctor` rake task
  stub. No working functionality yet -- see docs/authentication-gem-plan.md
  in the workspace repo.
