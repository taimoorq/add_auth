<h1 id="latchkey-title">Verify it’s you</h1>
<p>Confirm your identity to <%= @rule&.label || "continue" %>.</p>
<% if @error %><p role="alert"><%= @error %></p><% end %>
<% if Array(@methods).include?(:password) %>
  <%= form_with url: "/reauthenticate/password", data: latchkey_challenge_form_data(:reauthenticate) do |form| %>
    <%= form.hidden_field :purpose, value: @purpose %>
    <div class="<%= latchkey_class(:field) %>">
      <%= form.label :password, "Current password" %>
      <%= form.password_field :password, required: true, autocomplete: "current-password", class: latchkey_class(:input) %>
    </div>
    <%= latchkey_challenge_tag :reauthenticate, id: :password %>
    <%= form.submit "Verify with password", class: latchkey_class(:button), data: {latchkey_challenge_target: "submit"} %>
  <% end %>
<% end %>
<% if Array(@methods).include?(:email_link) %>
  <%= form_with url: "/reauthenticate/email", data: latchkey_challenge_form_data(:reauthenticate) do |form| %>
    <%= form.hidden_field :purpose, value: @purpose %>
    <%= latchkey_challenge_tag :reauthenticate, id: :email %>
    <%= form.submit "Email me a verification link", class: latchkey_class(:button), data: {latchkey_challenge_target: "submit"} %>
  <% end %>
<% end %>
<% if Array(@methods).empty? %><p role="status">No permitted verification method is available here. Contact your account administrator for an allowed way to continue.</p><% end %>
<p><%= link_to "Cancel", @rule&.return_to || "/", class: latchkey_class(:link) %></p>

<% if Array(@methods).include?(:passkey) %>
  <%= render "latchkey/passkeys/controls", mode: "get", options_url: "/reauthenticate/passkey/options", finish_url: "/reauthenticate/passkey", label: "Verify with a passkey", action: :reauthenticate, purpose: @purpose %>
<% end %>
