# AddAuth assets

The current key mark is `add_auth-mark.svg`, with reverse and monochrome variants.
The docs render the AddAuth wordmark as accessible text beside this mark.

`legacy-latchkey/` preserves the original supplied assets, including their
content credentials. These historical files are not used by current docs and
are not included in the gem package.
